<%= form_with(model: fat_percentage) do |form| %>
  <% if fat_percentage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fat_percentage.errors.count, "error") %> prohibited this fat_percentage from being saved:</h2>

      <ul>
        <% fat_percentage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :percent, style: "display: block" %>
    <%= form.number_field :percent %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
